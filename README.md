# Bash Internal Network Port Scanner

A lightweight bash script for scanning internal network IPs and ports from a compromised host. Useful during internal pentests or red teaming.

## Usage

```bash
chmod +x internal_scan.sh
./internal_scan.sh
